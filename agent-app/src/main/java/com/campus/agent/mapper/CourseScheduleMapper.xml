<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.agent.mapper.CourseScheduleMapper">
    <resultMap id="BaseResultMap" type="com.campus.agent.entity.CourseSchedule">
        <id property="id" column="id"/>
        <result property="className" column="class_name"/>
        <result property="weekday" column="weekday"/>
        <result property="courseName" column="course_name"/>
        <result property="startTime" column="start_time"/>
        <result property="endTime" column="end_time"/>
        <result property="location" column="location"/>
        <result property="teacher" column="teacher"/>
        <result property="weeks" column="weeks"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findByClassAndWeekday" resultMap="BaseResultMap">
        SELECT * FROM course_schedule
        WHERE class_name = #{className} AND weekday = #{weekday}
        ORDER BY start_time
    </select>
    <select id="findByClass" resultMap="BaseResultMap">
        SELECT * FROM course_schedule WHERE class_name = #{className} ORDER BY weekday, start_time
    </select>
    <select id="findAllClassNames" resultType="string">
        SELECT DISTINCT class_name FROM course_schedule
    </select>
</mapper>
