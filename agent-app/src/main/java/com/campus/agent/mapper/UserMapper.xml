<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.agent.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="com.campus.agent.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="nickname" column="nickname"/>
        <result property="avatar" column="avatar"/>
        <result property="email" column="email"/>
        <result property="phone" column="phone"/>
        <result property="role" column="role"/>
        <result property="studentClass" column="student_class"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM user WHERE id = #{id}
    </select>
    <select id="findByUsername" resultMap="BaseResultMap">
        SELECT * FROM user WHERE username = #{username}
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password_hash, nickname, avatar, email, phone, role, student_class)
        VALUES (#{username}, #{passwordHash}, #{nickname}, #{avatar}, #{email}, #{phone}, #{role}, #{studentClass})
    </insert>
    <update id="update">
        UPDATE user SET nickname=#{nickname}, avatar=#{avatar}, email=#{email}, phone=#{phone}
        WHERE id=#{id}
    </update>
    <select id="findAll" resultMap="BaseResultMap">
        SELECT * FROM user
    </select>
</mapper>
